---
name: Sync labels

on:
  push:
    branches:
      - main
    paths:
      - .github/labels.yml
  schedule:
    - cron: "34 5 * * *"
  workflow_call:
  workflow_dispatch:

permissions:
  issues: write

jobs:
  SyncLabels:
    uses: ivuorinen/actions/sync-labels@main
