---
name: PR Lint

on:
  push:
    branches-ignore: [master, main]
  pull_request:
    branches: [master, main]

jobs:
  pr-lint:

    permissions:
      contents: read
      packages: read
      statuses: write

    uses: ivuorinen/actions/pr-lint@main
