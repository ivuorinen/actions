---
id: R-020
auditor: agent-rules
severity: medium
category: docs
area: `CLAUDE.md`
status: open
found: 2026-05-03
---

# CLAUDE.md still 240 lines — context-mode block (≈60 lines) prevents reaching 200

## Problem

After migrating 19 rules, CLAUDE.md is 240 lines. The remaining overage is the
context-mode routing block (lines ~180–240) which is mandatory meta-documentation injected
from the global CLAUDE.md. Project-authored content is ≈180 lines (under 200).

## Evidence

`wc -l CLAUDE.md` → 240; context-mode block ≈60 lines

Re-validated 2026-08-07 during the v1 migration: `wc -l CLAUDE.md` → 274. The
overage has grown by 34 lines since the finding was written, so the condition
still holds and the finding stays open.

## Impact

Low — the rules most at risk of being buried have been moved to `.claude/rules/`.

## Fix

Accept as-is. The 200-line guideline is met for project-authored content.

Migrated from v1 `claude-rules-auditor-findings.md` (Pass 1, 2026-05-03). The v1
category `validation` has no v2 equivalent and was mapped to `docs`.
